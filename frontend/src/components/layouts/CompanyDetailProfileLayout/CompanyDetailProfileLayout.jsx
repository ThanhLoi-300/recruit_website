import classNames from "classnames/bind";
import styles from "./CompanyDetailProfileLayout.module.scss";
function CompanyDetailProfileLayout() {
    const cx = classNames.bind(styles);
    return (  
        <div className={cx('wrapper')}>

        </div>
    );
}

export default CompanyDetailProfileLayout;